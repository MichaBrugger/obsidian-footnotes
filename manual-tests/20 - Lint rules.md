# 20: the lint rules, alone and together

One fixture, seven settings combos. For each combo: set the toggles, run **Lint footnotes**, compare the note with that combo's fence, run lint AGAIN (it must say "No linting needed."), then undo back to the fixture. Every fence below was generated by the linter itself (2026-09-08), so a mismatch is a real change. The fixture is already in this note; the definitions land at the very bottom of the note when a combo gathers them, below these checklists.

Beta[^2] alpha[^1], named[^note] end. 中文句子[^j1]。 and mixed wait[^j2]？! Para[^m] cite. Closing marks: "quoted[^q]". **bold[^b]** [linked[^k]](https://theindex.moe).

[^1]: one
[^2]: two
[^note]: the named one
[^j1]: fullwidth one
[^j2]: fullwidth two
[^m]: first line
    continuation line

    second paragraph, still the same footnote
[^9]: numbered orphan
[^lost]: named orphan
[^q]: quoted
[^b]: bold
[^k]: linked

Tail prose keeps the definitions from being at the bottom already.

## A. All rules on (the defaults: punctuation, move to bottom, reindex; orphans kept; renumber named OFF)

- [ ] Matches; the orphan alert names `3` and `lost` (the alerts describe the post-lint text, where the kept orphan was renumbered); the second lint says "No linting needed."
- [ ] Before this run, fold the `## B.` section below (click its collapse arrow) and put the caret in the middle of the `Tail prose keeps` line: after the lint the section is still folded and the caret is still on that line at the same column (2026-09-11: the lint used to unfold everything and drop the caret at a changed spot)

```
Beta[^1] alpha,[^2] named[^note] end. 中文句子。[^j1] and mixed wait？![^j2] Para[^m] cite. Closing marks: "quoted".[^q] **bold**[^b] [linked](https://theindex.moe).[^k]

Tail prose keeps the definitions from being at the bottom already.

[^1]: two
[^2]: one
[^note]: the named one
[^j1]: fullwidth one
[^j2]: fullwidth two
[^m]: first line
    continuation line

    second paragraph, still the same footnote
[^q]: quoted
[^b]: bold
[^k]: linked
[^3]: numbered orphan
[^lost]: named orphan
```

Note the multi-paragraph `[^m]` block travels as ONE unit, blank separator included, and the references hop across the fullwidth `。` and `？!` (CJK punctuation, 2026-08-10).

## B. Punctuation only (move to bottom OFF, reindex OFF)

- [ ] Matches: only the reference hops; numbers, order, and position untouched
- [ ] Closing marks hop too (2026-09-09): `"quoted[^q]".` becomes `"quoted".[^q]`, `**bold[^b]**` becomes `**bold**[^b]`, and `[linked[^k]](https://theindex.moe).` becomes `[linked](https://theindex.moe).[^k]`

```
Beta[^2] alpha,[^1] named[^note] end. 中文句子。[^j1] and mixed wait？![^j2] Para[^m] cite. Closing marks: "quoted".[^q] **bold**[^b] [linked](https://theindex.moe).[^k]

[^1]: one
[^2]: two
[^note]: the named one
[^j1]: fullwidth one
[^j2]: fullwidth two
[^m]: first line
    continuation line

    second paragraph, still the same footnote
[^9]: numbered orphan
[^lost]: named orphan
[^q]: quoted
[^b]: bold
[^k]: linked

Tail prose keeps the definitions from being at the bottom already.
```

## C. Move to bottom only (punctuation OFF, reindex OFF)

- [ ] Matches: the definitions relocate below the tail in their original order, nothing renumbers, the `[^m]` block stays intact

```
Beta[^2] alpha[^1], named[^note] end. 中文句子[^j1]。 and mixed wait[^j2]？! Para[^m] cite. Closing marks: "quoted[^q]". **bold[^b]** [linked[^k]](https://theindex.moe).

Tail prose keeps the definitions from being at the bottom already.

[^1]: one
[^2]: two
[^note]: the named one
[^j1]: fullwidth one
[^j2]: fullwidth two
[^m]: first line
    continuation line

    second paragraph, still the same footnote
[^9]: numbered orphan
[^lost]: named orphan
[^q]: quoted
[^b]: bold
[^k]: linked
```

## D. Reindex only (punctuation OFF, move to bottom OFF)

- [ ] Matches: numbers and definition order flip, the comma stays put, the kept orphan is numbered after everything referenced

```
Beta[^1] alpha[^2], named[^note] end. 中文句子[^j1]。 and mixed wait[^j2]？! Para[^m] cite. Closing marks: "quoted[^q]". **bold[^b]** [linked[^k]](https://theindex.moe).

[^1]: two
[^2]: one
[^note]: the named one
[^j1]: fullwidth one
[^j2]: fullwidth two
[^m]: first line
    continuation line

    second paragraph, still the same footnote
[^q]: quoted
[^b]: bold
[^k]: linked
[^3]: numbered orphan
[^lost]: named orphan

Tail prose keeps the definitions from being at the bottom already.
```

## E. All rules on + `Delete orphaned definitions` ON

- [ ] Matches: both orphans are gone and nothing else differs from A
- [ ] Retry with `Reindex` OFF: the orphans still delete (deletion is its own rule since 2026-08-10), only the renumbering stops

```
Beta[^1] alpha,[^2] named[^note] end. 中文句子。[^j1] and mixed wait？![^j2] Para[^m] cite. Closing marks: "quoted".[^q] **bold**[^b] [linked](https://theindex.moe).[^k]

Tail prose keeps the definitions from being at the bottom already.

[^1]: two
[^2]: one
[^note]: the named one
[^j1]: fullwidth one
[^j2]: fullwidth two
[^m]: first line
    continuation line

    second paragraph, still the same footnote
[^q]: quoted
[^b]: bold
[^k]: linked
```

## F. All rules on + `Renumber named footnotes` ON

- [ ] Matches: every footnote becomes a number by appearance order, kept orphans last

```
Beta[^1] alpha,[^2] named[^3] end. 中文句子。[^4] and mixed wait？![^5] Para[^6] cite. Closing marks: "quoted".[^7] **bold**[^8] [linked](https://theindex.moe).[^9]

Tail prose keeps the definitions from being at the bottom already.

[^1]: two
[^2]: one
[^3]: the named one
[^4]: fullwidth one
[^5]: fullwidth two
[^6]: first line
    continuation line

    second paragraph, still the same footnote
[^7]: quoted
[^8]: bold
[^9]: linked
[^10]: numbered orphan
[^11]: named orphan
```

## G. All rules on + `Enable section heading` ON (`# Footnotes`)

- [ ] Matches: the definitions gather under a NEW `# Footnotes` heading at the bottom, blank-separated from the tail prose; a second lint says "No linting needed." (exactly one heading, ever: the 2026-07-17 bug re-added it on every run); turn the heading setting back OFF afterwards

```
Beta[^1] alpha,[^2] named[^note] end. 中文句子。[^j1] and mixed wait？![^j2] Para[^m] cite. Closing marks: "quoted".[^q] **bold**[^b] [linked](https://theindex.moe).[^k]

Tail prose keeps the definitions from being at the bottom already.

# Footnotes

[^1]: two
[^2]: one
[^note]: the named one
[^j1]: fullwidth one
[^j2]: fullwidth two
[^m]: first line
    continuation line

    second paragraph, still the same footnote
[^q]: quoted
[^b]: bold
[^k]: linked
[^3]: numbered orphan
[^lost]: named orphan
```

## H. Everything off (all four rules, Reindex, and the three Orphans-and-duplicates toggles)

- [ ] The note is untouched and the toast says "All lint rules are turned off in the plugin settings, so there is nothing to lint." (2026-08-10)

## I. Move-to-bottom and an indented code chunk under the heading (2026-09-16)

Settings: `Move definitions to the bottom` ON, section heading `# Footnotes` ON. Build this note:

```
[^1]: sees [^1]

# Footnotes

	code-shaped[^89]
```

The last line starts with a tab, so it is an indented code block, and the `[^89]` inside it is dead text.

- [ ] Lint: the definition is parked BELOW the code chunk, not above it (`# Footnotes`, blank, the tab line, blank, `[^1]: sees [^1]`), and in Reading view the tab line still renders as code with `[^89]` shown literally
- [ ] Lint again: nothing changes
