# 20: the lint rules, alone and together

One fixture, seven settings combos. For each combo: set the toggles,
run **Lint footnotes**, compare the note with that combo's fence, run
lint AGAIN (it must say "No linting needed."), then undo back to the
fixture. Every fence below was generated by the linter itself
(2026-09-08), so a mismatch is a real change. The fixture is already
in this note; the definitions land at the very bottom of the note when
a combo gathers them, below these checklists.

Beta[^2] alpha[^1], named[^note] end. 中文句子[^j1]。 and mixed wait[^j2]？! Para[^m] cite.

[^1]: one
[^2]: two
[^note]: the named one
[^j1]: fullwidth one
[^j2]: fullwidth two
[^m]: first line
    continuation line

    second paragraph, still the same footnote
[^9]: numbered orphan
[^lost]: named orphan

Tail prose keeps the definitions from being at the bottom already.

## A. All rules on (the defaults: punctuation, move to bottom, reindex; orphans kept; renumber named OFF)

- [ ] Matches; the orphan alert names `9` and `lost`; the second lint says "No linting needed."

```
Beta[^1] alpha,[^2] named[^note] end. 中文句子。[^j1] and mixed wait？![^j2] Para[^m] cite.

Tail prose keeps the definitions from being at the bottom already.

[^1]: two
[^2]: one
[^note]: the named one
[^j1]: fullwidth one
[^j2]: fullwidth two
[^m]: first line
    continuation line

    second paragraph, still the same footnote
[^3]: numbered orphan
[^lost]: named orphan
```

Note the multi-paragraph `[^m]` block travels as ONE unit, blank separator included, and the references hop across the fullwidth `。` and `？!` (CJK punctuation, 2026-08-10).

## B. Punctuation only (move to bottom OFF, reindex OFF)

- [ ] Matches: only the reference hops; numbers, order, and position untouched

```
Beta[^2] alpha,[^1] named[^note] end. 中文句子。[^j1] and mixed wait？![^j2] Para[^m] cite.

[^1]: one
[^2]: two
[^note]: the named one
[^j1]: fullwidth one
[^j2]: fullwidth two
[^m]: first line
    continuation line

    second paragraph, still the same footnote
[^9]: numbered orphan
[^lost]: named orphan

Tail prose keeps the definitions from being at the bottom already.
```

## C. Move to bottom only (punctuation OFF, reindex OFF)

- [ ] Matches: the definitions relocate below the tail in their original order, nothing renumbers, the `[^m]` block stays intact

```
Beta[^2] alpha[^1], named[^note] end. 中文句子[^j1]。 and mixed wait[^j2]？! Para[^m] cite.

Tail prose keeps the definitions from being at the bottom already.

[^1]: one
[^2]: two
[^note]: the named one
[^j1]: fullwidth one
[^j2]: fullwidth two
[^m]: first line
    continuation line

    second paragraph, still the same footnote
[^9]: numbered orphan
[^lost]: named orphan
```

## D. Reindex only (punctuation OFF, move to bottom OFF)

- [ ] Matches: numbers and definition order flip, the comma stays put, the kept orphan is numbered after everything referenced

```
Beta[^1] alpha[^2], named[^note] end. 中文句子[^j1]。 and mixed wait[^j2]？! Para[^m] cite.

[^1]: two
[^2]: one
[^note]: the named one
[^j1]: fullwidth one
[^j2]: fullwidth two
[^m]: first line
    continuation line

    second paragraph, still the same footnote
[^3]: numbered orphan
[^lost]: named orphan

Tail prose keeps the definitions from being at the bottom already.
```

## E. All rules on + `Delete orphaned definitions` ON

- [ ] Matches: both orphans are gone and nothing else differs from A
- [ ] Retry with `Reindex` OFF: the orphans still delete (deletion is its own rule since 2026-08-10), only the renumbering stops

```
Beta[^1] alpha,[^2] named[^note] end. 中文句子。[^j1] and mixed wait？![^j2] Para[^m] cite.

Tail prose keeps the definitions from being at the bottom already.

[^1]: two
[^2]: one
[^note]: the named one
[^j1]: fullwidth one
[^j2]: fullwidth two
[^m]: first line
    continuation line

    second paragraph, still the same footnote
```

## F. All rules on + `Renumber named footnotes` ON

- [ ] Matches: every footnote becomes a number by appearance order, kept orphans last

```
Beta[^1] alpha,[^2] named[^3] end. 中文句子。[^4] and mixed wait？![^5] Para[^6] cite.

Tail prose keeps the definitions from being at the bottom already.

[^1]: two
[^2]: one
[^3]: the named one
[^4]: fullwidth one
[^5]: fullwidth two
[^6]: first line
    continuation line

    second paragraph, still the same footnote
[^7]: numbered orphan
[^8]: named orphan
```

## G. Everything off (all three rules, Reindex, and both Orphans toggles)

- [ ] The note is untouched and the toast says "All lint rules are turned off in the plugin settings, so there is nothing to lint." (2026-08-10)
