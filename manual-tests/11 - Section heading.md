# 11: the footnote section heading

Automated coverage: 8 former checks now live in test/sheet-11-section-heading.test.ts and the smoke suite; run `npm test` and `npm run test:smoke` before this sheet.

Nothing here needs a person any more: every check this sheet made was about the text the plugin writes, and all eight are pinned by tests. There is nothing to tick.
