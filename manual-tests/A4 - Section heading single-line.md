# A4: section heading, single line

Settings: `Enable section heading` ON, heading `# Footnotes`, trim blank lines ON.

- [ ] First footnote inserted below creates `# Footnotes` above its definition at the note's end
- [ ] Second footnote appends below the first definition, no second heading

Insert into this sentence twice.

QOL: undo everything, type a `# Footnotes` heading yourself somewhere mid-note with prose after it, then insert again.

- [ ] The first definition slots under YOUR heading instead of creating a duplicate at the end
- [ ] A blank line separates the new definition from the prose below it, so the prose does NOT render as part of the footnote (bug fixed 2026-07-20)
- [ ] The same holds when a definition is appended after a mid-note definition group with content directly below
