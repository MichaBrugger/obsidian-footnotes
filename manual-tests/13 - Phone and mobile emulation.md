# 13: phone and mobile-emulation checks

Automated coverage: 0 former checks moved, because every check here needs the phone itself; what the plugin decides underneath checks 3, 4 and 5 is pinned in test/hunt/bug-rename-menu-needs-caret-inside.test.ts and test/manual-phone-and-mobile.test.ts; run `npm test` and `npm run test:smoke` before this sheet.

Run this sheet after a beta release has synced to the phone (BRAT beta in the normal vault); these checks can't run against the sandbox vault's dev build. The items marked "or emulation" have a desktop stopgap in mobile emulation (`app.emulateMobile(true)`, which reloads the window; run it again with `false` to leave), but the real phone is the ground truth. Every fixture is already in this note.

Fixture reference[^menu] for the long-press check.

- [ ] PHONE: the dialogs are bottom sheets like Obsidian's own **Rename heading** (2026-09-11; the earlier version pinned to the top of the screen is gone): run **Set footnote prefix**. The sheet rises from the bottom with rounded top corners and no close cross: the explanation, the field, the **Save** button, and a **Cancel** button stacked under it. Tap into the field: with the keyboard open, the whole sheet (field, inline error, both buttons) sits above the keyboard, nothing hidden behind it, the same lift Obsidian gives its Rename heading sheet (Android report 2026-08-28). The base is shared, so spot-check one of the Rename / Name-the-footnote dialogs the same way
- [ ] PHONE: long-press on `[^menu]` above: Obsidian's own menu opens with only its **Delete footnote and reference** item. No **Rename footnote** there, and none is possible: on a phone Obsidian builds that menu alone and gives plugins no way to add to it (2026-09-11; the desktop right-click menu is sheet 07). Dismiss it
- [ ] PHONE or emulation: rename after a long press: long-press `[^menu]` so the word is selected, dismiss the menu, then tap the **Rename footnote** toolbar icon: the dialog opens prefilled with `menu`. What is being judged is that the gesture and the toolbar tap reach the command at all; which selection shapes resolve to `menu` is pinned in the unit tests (2026-09-11)
- [ ] PHONE: long-press on the `[^menu]:` label at the bottom of this note: no menu opens (Obsidian shows none for a label on a phone). With the label's word still selected, tap the **Rename footnote** toolbar icon: the dialog opens prefilled with `menu`. Cancel
- [ ] PHONE: Rename footnote from the toolbar with a refused name: type `two words` and tap the button; the error shows and the keyboard stays up (or comes straight back) with the field focused and its text selected, so the fix can be typed at once (2026-09-11; the name to type used to be `menu` itself, which the plugin reads as "no change" and which closes the dialog with no error at all)
- [ ] PHONE: open the popup on `[^menu]` above (`Edit footnotes in a popup` ON): the label `[^menu]:` and the body text are the same size and sit on the same line, on your usual theme and on Minimal (2026-09-11; the label copies the editor's own text metrics)
- [ ] PHONE, Minimal theme: the Set footnote prefix sheet shows no box or outline around the field or the buttons, only the field's own border (2026-09-11; the dialog no longer uses settings rows, which Minimal boxed; switch back to your theme afterwards)
- [ ] PHONE or emulation: rapid popup entry, `Edit footnotes in a popup` ON: three fast rounds of press, type a few characters, press to close, press again; every definition below ends up with exactly the text typed for it, closing feels immediate, and the console (if reachable) shows no `Cannot read properties of undefined (reading 'split')` errors (the 2026-08-13 crash hit both modes; the desktop twin is sheet 02)
- [ ] PHONE or emulation: the plugin's commands sit on the mobile toolbar with their own icons and each one works from a tap (the popup stays open when a toolbar tap launches a command)

[^menu]: the definition to rename
