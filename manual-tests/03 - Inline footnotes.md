# 03: inline footnotes

Automated coverage: 9 former checks now live in test/sheet-03-inline-footnotes.test.ts and the smoke suite; run `npm test` and `npm run test:smoke` before this sheet.

Nothing on this sheet needs a human in the real app any more: every check was about the text a press writes, where it leaves the caret, or whether the clipboard was read, and the automated layers see all three. There is nothing to tick here.
