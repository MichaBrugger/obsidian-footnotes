---
decoy: this frontmatter mentions [^1] and must never be touched
---

# Hardening batch, 2026-08-10

> [!info] How to use
> One section per fix cluster from the 2026-08-10 session. Each section carries its own fixture text; run the listed command on it, check, undo. Default settings unless the section says otherwise.

## Names: backticks warned, dollars fine

Press a footnote key inside the detail-less backticked reference [^ba`ck] and inside the dollar one [^a$1].

- [ ] Inside `[^ba` + backtick + `ck]`: a toast says the name contains backticks and won't render; nothing is inserted
- [ ] Inside `[^a$1]`: the press CREATES its definition normally (dollar names are valid; verified they render)
- [ ] Autonumber next to `pay[^a$1] and[^b$2]` does not treat the span between the dollars as math: both stay footnotes and render

## Navigation with tricky names and places

Refs: case[^Note] and callout below.

> [!note] A callout
> body[^cq] here
> [^cq]: callout definition

[^note]: lowercase definition for the uppercase reference

- [ ] Press inside `[^Note]`: navigates to the LOWERCASE `[^note]:` definition (ids fold case; no duplicate created)
- [ ] Press inside `[^cq]`: navigates to the definition INSIDE the callout (no duplicate at the bottom, 2026-08-10)
- [ ] Press on the callout's `[^cq]:` line: jumps back to `body[^cq]`
- [ ] Lint: the callout definition stays in place, its label is NOT mangled by the punctuation rule, and `[^cq]` is never called an orphan

## Case-folded prefix (prefix feature ON, property `P-`)

Set `footnote-prefix: P-` via the command, then with `hand[^p-1] typed` in the note (plus `[^p-1]: one` at the bottom):

- [ ] The autonumbered command mints `[^P-2]`, NOT a colliding `[^P-1]`
- [ ] `footnote-prefix: 2. # a comment` in frontmatter works: inserts use prefix `2.` and nothing complains about a phantom value (YAML comments stripped, 2026-08-10)
- [ ] `footnote-prefix:2.` (no space after the colon) is IGNORED — Obsidian shows no property, the plugin reads none

## Escapes and inline footnotes are not references

Literal \[^9] escaped, and an inline one ^[^inline-content] here, with a real[^r] reference.

[^r]: real definition

- [ ] Lint/reindex renumbers only `[^r]`; the escaped `\[^9]` and the `^[…]` inline content are byte-for-byte untouched
- [ ] Autonumber after this section mints the next number as if `[^9]` did not exist

## Protected regions: math, indented code, comments, fences

Math inline $x[^9].$ stays, display too:

$$
[^8]: mathematical label
y[^7]
$$

    indented code[^90] block, standalone

Comment boundaries: live[^c1] <!-- hidden [^c2]
--> live again[^c3], short form <!--> done.

> ```
> quoted fence[^f1]

- ```
  listed fence[^f2]
  ```

Real refs to lint: swap me[^s1].

[^c1]: one
[^c3]: three
[^s1]: swap definition

- [ ] Lint: everything inside `$…$`, `$$…$$`, the indented block, the comment, and BOTH fences is byte-for-byte untouched; only `swap me[^s1].` swaps to `swap me.[^s1]`
- [ ] Autonumber: none of `[^9]`, `[^8]`, `[^7]`, `[^90]`, `[^c2]`, `[^f1]`, `[^f2]` reserve numbers
- [ ] The refs BEFORE the comment opener and AFTER its closer (`[^c1]`, `[^c3]`) count as live: they renumber/swap like normal text (2026-08-10)
- [ ] The `> ```' fence above ended when its blockquote did — this checklist text is live, not phantom code

## CJK punctuation

中文句子[^j1]。 and mixed wait[^j2]？!

[^j1]: jp one
[^j2]: jp two

- [ ] Lint swaps across the fullwidth stop: `[^j1]。` becomes `。[^j1]`, and `[^j2]？!` crosses both
- [ ] With insert-at-end-of-word ON, autonumber mid-word in `中文。` lands the reference AFTER the `。`

## Tables: escaped pipes

| Head |
| ---- |
| left \| right[^tp] |

- [ ] Click into the cell, caret just inside `[^tp]` (after the `\|`), press the named key: it CONTINUES the footnote (creates the definition) instead of nesting a new `[^]` into it

## Reading view and the popup (popup ON, lint-on-creation ON)

- [ ] Create a footnote (popup opens), switch to Reading view while the popup is up, close the popup with the hotkey: the note text is NOT edited by the deferred lint (check in source view after)
- [ ] Vim users: Esc that exits insert mode inside the popup does NOT also close the popup; a second Esc closes it

## Lint stability (paranoia checks)

`def at top[^t1]` scenario: put `[^t1]: def` as the FIRST line of a scratch note with `---` on the line right below, a `text[^t1]` line, another `---`, and prose after.

- [ ] Lint moves the definition down WITHOUT turning the top of the note into frontmatter (no swallowed prose; a blank line appears above the first `---`)
- [ ] With lint-on-save ON, saving a note twice in a row never rewrites it the second time (no infinite churn)
