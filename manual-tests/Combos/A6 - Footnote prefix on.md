---
footnote-prefix: 7.
---

# A6: per-note footnote prefix ON

Settings: `Per-note footnote prefix` ON.

This note already carries one prefixed footnote[^7.1].

- [ ] Auto-numbered hotkey inserts `[^7.2]` in this sentence
- [ ] Again right after the new marker chains `[^7.3]`
- [ ] Named: type `[^tag]`, press the named hotkey inside it — the marker becomes `[^7.tag]` and its detail is created as `[^7.tag]:` (bug fixed 2026-07-20)
- [ ] A name already carrying the prefix (type `[^7.own]`) is left as is
- [ ] Toggle OFF: the same hotkeys insert plain `[^1]` and leave `[^tag]` unprefixed

[^7.1]: the first prefixed footnote
