# 13: a divider + heading pair that already sits mid-note

Automated coverage: 4 former checks now live in test/sheet-13-divider-heading-already-in-the-note.test.ts and the smoke suite; run `npm test` and `npm run test:smoke` before this sheet.

Nothing here needs a person any more: every check this sheet made was about the note's text after an insert or a lint, and all four are pinned by tests on this sheet's own fixture. There is nothing to tick.
