# 01: numbered footnotes

Automated coverage: 12 former checks now live in test/sheet-01-numbered-footnotes.test.ts and the smoke suite; run `npm test` and `npm run test:smoke` before this sheet.

Nothing on this sheet needs a human in the real app any more: every check was a statement about the text a press writes or where it leaves the caret, and both of those are settled by the automated layers. There is nothing to tick here.
