# 09: multiple cursors (2026-08-22)

Automated coverage: all 12 former checks now live in test/sheet-09-*.test.ts, test/multi-caret.test.ts and the smoke suite; run `npm test` and `npm run test:smoke` instead of this sheet.

Nothing here needs a human in the real app: every check was about the text, the carets or a toast's wording, and the fake editor sees all three.
