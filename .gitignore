# Intellij
*.iml
.idea

# npm
node_modules

# build (main.js ships in releases, not in the repo)
main.js
*.js.map

# plugin userdata
data.json

# macOS metadata files
**.DS_Store
# Inkscape auto-backup copies (timestamped crash-recovery files)
icons/*.svg.*.svg

# claude skill-eval scaffolding and agent worktrees
.claude/skills/hunt-bugs-workspace/
.claude/worktrees/
scripts/.smoke-settings.bak.json

# Stryker mutation testing (local pre-release audit — npm run mutation)
.stryker-tmp/
reports/
stryker.log

# coverage output and mutation-survivor working dumps
coverage/
survivors-*.json
